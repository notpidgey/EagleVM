#include "eaglevm-core/virtual_machine/il/handlers/vm/vm_rflags_save.h"

namespace eagle::il::handlers
{
    il_insts vm_rflags_save::gen_il(reg_size size, uint8_t operands)
    {
        il_insts insts;

        return insts;
    }
}
