#include "eaglevm-core/virtual_machine/il/handlers/vm/vm_exit.h"

namespace eagle::il::handlers
{
    il_insts vm_exit::gen_il(reg_size size, uint8_t operands)
    {
        il_insts insts;

        return insts;
    }
}
