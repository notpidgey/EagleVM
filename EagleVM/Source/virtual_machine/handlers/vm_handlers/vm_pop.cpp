#include "virtual_machine/handlers/vm_handlers/vm_pop.h"

vm_pop_handler::vm_exit_handler()
{
}

handle_instructions vm_pop_handler::construct_single(reg_size size)
{
    return handle_instructions();
}
